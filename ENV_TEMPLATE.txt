# ============================================
# PRODWAY AI / SOWFLOW - ENVIRONMENT VARIABLES
# Rename this to .env and fill in values
# ============================================

# ENVIRONMENT
APP_ENV=development
APP_URL=https://api.prodway.ai

# --------------------------------------------
# 1. AI PROVIDERS (At least one required)
#
# GOOGLE_API_KEY: Gemini 2.0 Flash Lite (cheap default)
#   Get from: https://aistudio.google.com/apikey
#   Cost: ~$0.075/1M tokens (very cheap)
#
# ANTHROPIC_API_KEY: Claude (higher quality, used for BYOK)
#   Get from: https://console.anthropic.com
#   Cost: ~$3/1M input, $15/1M output (more expensive)
#
# DEFAULT_AI_PROVIDER: Which to use when both are set
#   Options: gemini (default, cheap), anthropic (quality)
# --------------------------------------------
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
DEFAULT_AI_PROVIDER=gemini

# --------------------------------------------
# 2. SLACK — Marketplace Distribution (Required)
# Your Slack app at: https://api.slack.com/apps
#
# Go to Basic Information → copy these three values.
# NOTE: SLACK_BOT_TOKEN / SLACK_APP_TOKEN are NOT needed.
# Each workspace gets its own token via OAuth install.
# --------------------------------------------
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_SIGNING_SECRET=

# --------------------------------------------
# 3. STRIPE CONNECT (Per-customer invoicing)
# Platform keys from: https://dashboard.stripe.com/apikeys
# Connect Client ID from: https://dashboard.stripe.com/settings/connect
#
# Customers connect their OWN Stripe accounts via OAuth.
# Invoices are created on their accounts, not yours.
# --------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_CLIENT_ID=
STRIPE_WEBHOOK_SECRET=

# --------------------------------------------
# 4. DOCUSIGN (Per-customer e-signatures)
# Create app at: https://developers.docusign.com
#
# Customers connect their OWN DocuSign accounts via OAuth.
# SOWs are sent from their accounts for signature.
# --------------------------------------------
DOCUSIGN_INTEGRATION_KEY=
DOCUSIGN_SECRET_KEY=
# Use account-d.docusign.com for dev, account.docusign.com for prod
DOCUSIGN_AUTH_SERVER=account-d.docusign.com
# Use demo.docusign.net for dev, docusign.net for prod
DOCUSIGN_BASE_URL=https://demo.docusign.net/restapi

# --------------------------------------------
# 5. SENDGRID (Contact form emails)
# Get API key: https://app.sendgrid.com/settings/api_keys
# Free tier: 100 emails/day
# --------------------------------------------
SENDGRID_API_KEY=
CONTACT_TO_EMAIL=dale@prodway.ai
SENDGRID_FROM_EMAIL=noreply@prodway.ai

# --------------------------------------------
# 6. MERCURY (ACH Invoicing — lower fees for large invoices)
# Get API token: Mercury Dashboard → Settings → Developers → API Tokens
# Requires Mercury Plus or Pro plan
# --------------------------------------------
MERCURY_API_TOKEN=
MERCURY_ACCOUNT_ID=

# --------------------------------------------
# 6. DATA STORAGE (Optional — defaults to ./data)
# For production, mount a persistent volume
# --------------------------------------------
# DATA_DIR=/app/data

# --------------------------------------------
# 7. FORMPILOT API (separate deploy, e.g. Railway)
# Used by FormPilot Chrome extension for AI field mapping + Import from URL.
# Set in Railway app variables when deploying apps/formpilot-api.
#
# Required: At least one of GOOGLE_API_KEY or ANTHROPIC_API_KEY
# Optional: DEFAULT_AI_PROVIDER (gemini or anthropic)
# Railway sets PORT automatically.
# --------------------------------------------
