apiVersion: v1
kind: Service
metadata:
  name: dealflow
  namespace: prodway
  labels:
    app: dealflow
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: dealflow
