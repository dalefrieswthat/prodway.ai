apiVersion: v1
kind: Service
metadata:
  name: sowflow
  namespace: prodway
  labels:
    app: sowflow
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: sowflow
